{
  "name": "savdo-programma-desktop",
  "version": "1.0.0",
  "description": "Savdo Programma Desktop Application",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder",
    "build:win": "set CSC_IDENTITY_AUTO_DISCOVERY=false && electron-builder --win",
    "build:win:portable": "set CSC_IDENTITY_AUTO_DISCOVERY=false && electron-builder --win portable",
    "build:packager": "electron-packager . \"Savdo Programma\" --platform=win32 --arch=x64 --out=dist --overwrite --icon=assets/icon.ico",
    "dist": "electron-builder --publish=never"
  },
  "keywords": [
    "savdo",
    "inventory",
    "sales"
  ],
  "author": "Savdo Programma",
  "license": "MIT",
  "devDependencies": {
    "electron": "^39.2.7",
    "electron-builder": "^24.9.1",
    "electron-packager": "^17.1.2"
  },
  "build": {
    "appId": "com.savdo.programma",
    "productName": "Savdo Programma",
    "directories": {
      "output": "dist"
    },
    "files": [
      "main.js",
      "preload.js",
      "config.json",
      "renderer/**/*",
      "admin_panel/**/*",
      "seller_panel/**/*",
      "node_modules/**/*"
    ],
    "win": {
      "target": [
        {
          "target": "dir",
          "arch": [
            "x64"
          ]
        }
      ],
      "icon": "assets/icon.ico"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    },
    "portable": {
      "artifactName": "Savdo-Programma-Portable.exe"
    }
  }
}
